# Keeps the existing GitHub -> Netlify pipeline working unchanged.
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so the stubbed case-study routes resolve to the app.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
